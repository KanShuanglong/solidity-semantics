
contract C {
	 function f (int x) public returns (int y)  {
	 	  int sum;
		  if (x < 0 ) {
		     return 0;
		  }
		  else {
		    sum = x + functionCall(f ; x - 1);
		    return sum;
		  }

		  
	}
}

#execute(functionCall(new C(); f; 5))